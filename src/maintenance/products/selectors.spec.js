import * as selectors from './selectors';

describe('Maintenance Products Selectors', () => {
    const MAINTENANCE_DEFAULT_STATE = {
        productTemplates: [],
        productFeatures: {},
        selectedProductTemplates: [],
        selectedProductChecklist: []
    };

    describe('getChecklistSelector', () => {
        const { getChecklistSelector } = selectors;
    });

    describe('getProductTemplatesSelector', () => {
        const { getProductTemplatesSelector } = selectors;

    });

    describe('selectedProductTemplateSelector', () => {
        const { selectedProductTemplateSelector } = selectors;

    });

    describe('getProductFeaturesSelector', () => {
        const { getProductFeaturesSelector } = selectors;

    });

    describe('getProductFeaturesAsArraySelector', () => {
        const { getProductFeaturesAsArraySelector } = selectors;

    });

    describe('checkHasConfig', () => {
        const { checkHasConfig } = selectors;

    });
});